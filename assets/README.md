Assets used by `index.html`:

- `portrait-1.jpg` – `portrait-4.jpg` — hero circle photos (320×320, cropped square, ~17-20KB each). To replace one, keep the same filename and roughly the same square crop centered on the face so the circular frame still looks right.
- `og-image.jpg` — social share preview image (1200×630), shown when the site link is pasted into iMessage/Slack/Twitter/etc.

The logo is currently a text wordmark (see `.logo` in `css/styles.css`) — no image file yet.
